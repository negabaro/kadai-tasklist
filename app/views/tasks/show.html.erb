<h1>id: <%= @task.id %> のTask詳細ページ</h1>

<p><%= @task.content %></p>


<%= link_to '編集', edit_task_path(@task.id) %>
<%= link_to '新規投稿', new_task_path %>
<%= link_to '削除', task_path(@task.id), method: :delete, data: {confirm: '本当に削除してよろしいですか？' }%>
